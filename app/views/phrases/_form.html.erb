<%= form_with(model: phrase) do |form| %>
  <% if phrase.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(phrase.errors.count, "error") %> prohibited this phrase from being saved:</h2>

      <ul>
        <% phrase.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :chinese_simplified, style: "display: block" %>
    <%= form.text_field :chinese_simplified %>
  </div>

  <div class="field">
    <%= form.label :pinyin, style: "display: block" %>
    <%= form.text_field :pinyin %>
  </div>

  <div class="field">
    <%= form.label :english, style: "display: block" %>
    <%= form.text_field :english %>
  </div>

  <div class="field">
    <%= form.submit %>
  </div>
<% end %>
